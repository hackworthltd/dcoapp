name: Check DCO

on:
  pull_request:

jobs:
  dco:
    permissions:
      contents: read
      pull-requests: write
      statuses: write

    runs-on: ubuntu-latest

    steps:
      - name: Check DCO
        uses: hackworthltd/dcoapp@testing
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
